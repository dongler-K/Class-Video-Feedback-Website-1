
import React from "react";
import ReactDOM from "react-dom/client";
import FeedbackUploader from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FeedbackUploader />
  </React.StrictMode>
);
